# Archive

Git-kept (not deleted) dead/dev code and baseline artifacts.

- `phase0_baseline/` — pre-refactor contract/instrumentation baselines (OpenAPI, routes, prints) and Phase A gate artifacts. See `phase0_baseline/BASELINE.md`.
- `backend/...` — files removed from the live tree during Phase A (moved, not deleted): root `check.py`, `fix.py`, `test_dell.py`; `app/fix.py`, `app/patch_main.py`, `app/admin_ui.py`; `app/scratch/*`; `app/scripts/{seed_database,cleanup_duplicates,trigger_ztp_simulation,read_app_log,read_remote_main,ztp_bootstrap}.py`.
